[build]
builder = "dockerfile"
buildTarget = "production"

[deploy]
startCommand = "cd services/auth-service && npx prisma db push --skip-generate && node dist/main.js"
restartPolicyType = "on_failure"
restartPolicyMaxRetries = 3
